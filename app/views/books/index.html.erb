<h1>Books</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Body</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @books.each do |book| %>
      <tr>
        <td><%= book.title %></td>
        <td><%= book.body %></td>
        <td><%= link_to 'Show', book %></td>
        <td><%= link_to 'Edit', edit_book_path(book) %></td>
        <td><%= link_to 'Destroy', book, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>



<h2>New book</h2>

<%= form_for(@book) do |f| %>
   



  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :body %><br>
    <%= f.text_field :body %>
  </div>

    <%= f.submit %>

<% end %>
</body>

<!-- HTMLのinputタグとtextareaタグを使ってテキスト入力欄を表示する方法【初心者向け】
https://techacademy.jp/magazine/5864 -->


<!-- 新規投稿と投稿一覧を同じページに配列したい
https://teratail.com/questions/96547 -->

<!-- Railsで更新も編集も削除も、全部indexページでやりたい
https://qiita.com/jacoyutorius/items/ea0673fe30cfe7cdac8f -->