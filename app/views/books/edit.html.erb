<h1>Editing Book</h1>

<%= form_for(@book) do |f| %>

  <h4 class="TB">Title</h4>
    <%= f.text_field :title %>

  <h4 class="TB">Body</h4>
    <%= f.text_area :body %>

  <div class="UB"><%= f.submit %></div>

<%= link_to 'Show', book_path(@book.id) %>
|
<%= link_to 'Back', books_path %>

<% end %>